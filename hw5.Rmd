---
title: "hw5"
author: "Shan Jiang"
date: "11/2/2018"
output: github_document
---
```{r}
library(tidyverse)
library(rvest)

set.seed(1)
```

## Problem 1
Inspecting the file path
```{r}
file_path <- "./data/"
length(list.files(file_path))  # How many files are there?
list.files(file_path)[1:10]  # Show first 10 names
list.files(file_path)[11:20]  # Show last 10 names
```

Create a tidy dataframe containing data from all participants, including the subject ID, arm, and observations over time:


```{r}

read_sheet <- function(path) {
   readr::read_csv(str_c(path, "csv", sep = "."))
}

tb_base_con = "./data/con_"
tb_base_exp = "./data/exp_"
vec_con = str_c(c(tb_base_con, tb_base_exp), c('0':'10', '0':'10'))

sheet_df = map_df(vec_con, read_sheet)

sheet_df 
```

```{r}

vec_exp = str_c(tb_base_con, 1:10)

exp_df = map_df(vec_con, read_sheet)

bind_rows(
```


